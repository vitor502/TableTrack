// Este arquivo exporta tipos TypeScript que são utilizados em toda a aplicação, como interfaces para os dados dos clientes.

export interface Cliente {
    id: number;
    nome: string;
    email: string;
    telefone: string;
}